# terraform-vanilla-webserver
find it here:
https://registry.terraform.io/modules/scottwinkler/webserver/vanilla

usage
```
module "webserver" {
  source  = "scottwinkler/webserver/vanilla"
  version = "0.1.1"

  # insert the 1 required variable here
}
```